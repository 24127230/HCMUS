int i_line;
        // bool have_opt = 0;
        // while(i_line = a[i].opts.find(",")) {
        //     int opt = stoi(a[i].opts.substr(0, i_line));
        //     if (option[opt]) {
        //         option[opt]--;
        //         res[opt].push_back(a[i]);
        //         have_opt = 1;
        //         break;
        //     }
        //     a[i].opts.erase(0, i_line + 1);
        // }
        // if (!have_opt) res[0].push_back(a[i]);