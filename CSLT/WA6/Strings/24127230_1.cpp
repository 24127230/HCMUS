#include <iostream>
#include <string>
using namespace std;
int main()
{
    string s;
    getline(cin, s);
    // while(s.find(' ')<s.length()){
    //             s.erase(s.find(''))
    // }
    cout << s.find('a');

    return 0;
}